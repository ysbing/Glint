<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ysbing.samples.glint">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme"
        tools:targetApi="n">
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:screenOrientation="fullSensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:screenOrientation="fullSensor" />
        <activity
            android:name=".http.HttpRequestActivity"
            android:label="普通请求"
            android:screenOrientation="fullSensor" />
        <activity
            android:name=".http.HttpModuleRequestActivity"
            android:label="复杂请求"
            android:screenOrientation="fullSensor" />
        <activity
            android:name=".upload.UploadRequestActivity"
            android:label="上传请求"
            android:screenOrientation="fullSensor" />
        <activity
            android:name=".download.DownloadRequestActivity"
            android:label="下载请求"
            android:screenOrientation="fullSensor" />
        <activity
            android:name=".websocket.WebSocketRequestActivity"
            android:label="WebSocket请求"
            android:screenOrientation="fullSensor" />
    </application>

</manifest>